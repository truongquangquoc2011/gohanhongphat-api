export const QUERY_PARAMS = {
  ACCESS_TOKEN: 'accessToken',
  REFRESH_TOKEN: 'refreshToken',
  ERROR_MESSAGE: 'errorMessage',
};
export const COURSE_BASIC_SELECT = {
  id: true,
  status: true,
  isDelete: true,
  deletedAt: true,
} as const